INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR} ${AUDIO_INCLUDE_DIRS})
LINK_DIRECTORIES(${AUDIO_LIBRARY_DIRS})

ADD_EXECUTABLE(fileinfo EXCLUDE_FROM_ALL fileinfo.cpp)
TARGET_LINK_LIBRARIES(fileinfo ${UTILS_LIBRARIES} ${AUDIO_LIBRARIES})
